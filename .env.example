# Multisynq/Croquet API Configuration
# This file documents environment variables used by the application.
# For production, set these in your deployment environment or in a .env file (not committed to git).

# Fallback Croquet API Key
# Used when apiKey.js cannot be loaded
# Default: dev-only key (localhost only)
FALLBACK_API_KEY=2pRwCCAKq2s71AxVCputQdaLzjNCJGIQrGVKw1aaC6

# Note: The production unrestricted API key is loaded from apiKey.js which is:
# 1. Served locally via the web server
# 2. Not committed to version control
# 3. Configured separately in the deployment environment
